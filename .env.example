# Optional: connect a server-side LLM provider. Never prefix secrets with NEXT_PUBLIC_.
LLM_API_KEY=
LLM_MODEL=
# Optional. Raises GitHub API limits and allows deeper comment verification.
GITHUB_TOKEN=
